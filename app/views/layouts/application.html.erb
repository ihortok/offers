<!DOCTYPE html>
<html>
  <head>
    <title>Aronnax Offers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#603cba">
    <meta name="theme-color" content="#ffffff">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "layouts/partials/animated_background" unless user_signed_in? %>

    <%= render "layouts/partials/top_navbar" if show_top_navbar? %>
    <div class="container px-3 m-auto pt-4 pb-[73px] <%= "pt-[73px]" if show_top_navbar? %>">
      <% if user_signed_in? %>
        <%= render "shared/flashes" %>
        <%= yield %>
      <% else %>
        <div class="card py-4 bg-white relative z-10 lg:w-2/3 m-auto">
          <%= render "shared/flashes" %>
          <%= yield %>
        </div>
      <% end %>
    </div>
    <%= render "layouts/partials/bottom_navbar" if show_bottom_navbar? %>
  </body>
</html>
